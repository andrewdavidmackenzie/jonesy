[workspace]
resolver = "3"
members = [
    "jones",
    "examples/array_access",
    "examples/oom",
    "examples/panic",
    "examples/perfect",
]

[profile.panic]
inherits = "release"
debug = "line-tables-only"
# split-debuginfo - for macos
# overflow-checks = false ?